# 胜率优化 P5 报告（2026-09-10 20:40 自动化执行）

## 结论先行

**本轮不做新的策略参数改动**——1m + P4 过滤器的干净样本只有 37 笔已平仓订单，任何再调参都是拟合噪声。本轮价值在于：核实各防线全部生效、澄清 15m 对照实验的真相、把积累的已验证改动提交入库（29 文件，commit `4f7c2bb`）。

## P4 部署后数据（10:16 起，121 笔已平仓）

| 指标 | P4 前全量基线 | P4 后新批次 |
|---|---|---|
| 胜率 | 21.9% | **33.1%** |
| 均单净盈亏 | -1.18 USDT | **-1.02 USDT** |

方向正确但**尚未转正**。注意：这批数据被 15m 对照实验污染（见下）。

## 15m 对照实验真相（重要澄清）

上一轮记录的"并发协作者 15m 改动"实际运行于 10:16~14:00：
- 期间产生 105 笔 15m 订单，胜率 32.4%、均单 -0.96；
- 该分支把 maxHoldBars 设为 8（≈2 小时），94 笔 timeout 平仓全部来自这里（持仓 2~8 根），timeout 单毛利 +0.08 基本打平、纯付手续费；
- 14:00 已由协作者自行回退 1m 并写入决策注释（`server/research.js`），历史对照：1m 22.8%/-0.91、5m 14.0%/-3.69、15m 14.8%/-2.35，两次放大周期均被证伪。

## 各防线核验结果（全部生效）

1. **负期望币种拉黑**：全量最差币种（PONS -135/60单、DOT 10%胜率/40单 等 13 个）自 09-09 晚起全部停止出单 ✅
2. **P4 入场过滤器**（ATR 下限+分位、RSI 方向门槛、追高过滤）：胜率 21.9%→33.1% ✅
3. **代理健康探测**（proxyHealth）：运行中，日志正常 ✅
4. **PM2 加固 + logrotate**：restarts=0，无崩溃循环 ✅

## 结构性发现（供后续轮次参考）

- **止盈单是金子，止损单是失血点**：全量 270 笔止盈单净赚 +2727（均 +10.1），2142 笔止损单净亏 -5745（均 -2.68）。SL:TP 命中比 8:1 是负期望的结构性根源——**下一步主线仍是入场质量（回调入场/结构确认）**，等 1m 干净样本积累到 100+ 笔再动。
- **盈利需要时间**：持仓 30-59 根的订单胜率 58~60%、均单 +2.2~+2.8；<5 根的只有 4.9%。任何缩短持仓的自适应行为都要警惕（15m 分支的 maxHoldBars=8 就是反例）。
- **timeout 单是手续费漏斗**：毛利打平、纯付 ~0.5 U/单费用，减少无意义入场比调整出场更有效。

## 本轮操作清单

- `npm test`：129 用例，122 通过 / 0 失败 / 7 跳过 ✅
- git commit `4f7c2bb`：29 文件（P5 周期决策注释、proxyHealth、PM2 加固、K线完整性守卫、15m 文档、分析脚本）
- 服务：20:37 已重启且运行当前代码（ProxyHealth 日志确认），HTTP 200，**无需再次重启**（本轮无运行时改动）
- 未提交：`output/sanhe6/`（爬虫数据产物，非代码）

## 下轮执行建议

1. 先 `git status` 确认无并发改动（本轮已清空工作区，基线干净）。
2. 用 `node scripts/analyze-post-p4.cjs`（把 SINCE 改为 2026-09-10 14:00）复查 1m 干净样本；**≥100 笔后再考虑动入场逻辑**。
3. 样本不足时宁可不动，不要重复 5m/15m 的覆辙。
