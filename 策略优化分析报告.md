# 策略优化分析报告（2026-09-09）

> 范围：`server/localAnalysis.js`、`enhancedAnalysis.js`、`tradingSimulator.js`、`simulatedAccount.js`、`adaptiveFilters.js`、`adaptiveConfig.js`、`globalAutomation.js`、`paperAutomation.js`，以及 `LOW_WINRATE_ANALYSIS.md` 历史结论。
> 背景：历史 2,081 笔模拟单，胜率 22.1%，净亏 -1,978 USDT（数据已清零重置，无法复算，以下基于代码逻辑推断）。

---

## 🔴 P0-1：持仓复核的移动止损过紧 —— 最可能的主凶（新发现）

**位置**：`globalAutomation.js` `localProtectionReview()`（约 550 行起）

```js
const atr = rows.slice(-14)... / 14;          // 1m 周期的 14 根 ATR
const stopLoss = long ? Math.max(order.plan.stopLoss, price - 1.5 * atr) : ...;
const takeProfit = long ? Math.max(order.plan.takeProfit, price + 3 * atr) : ...;
```

问题链条：
1. 每轮复核都会执行，**进场后只要价格略涨，止损立刻被收紧到 `price - 1.5 × 1m ATR`**；
2. 1m 的 ATR 极小（通常为价格的 0.05%~0.15%），1.5 ATR ≈ 0.1%~0.2% 的贴身止损；
3. 加密市场正常波动噪音就能扫掉它 → **平均持仓 11 根 + 胜率 22% 与此完全吻合**；
4. 止盈 4 ATR 根本没机会兑现。

`LOW_WINRATE_ANALYSIS.md` 把原因归为"过早止损"，开的药方却是"开仓止损 2.5→3.5 ATR"——**方向错了：该修的是复核的 trailing 逻辑，不是开仓止损**。开仓止损放宽只会让每笔亏损单亏更多。

**建议**：
- trailing 距离 ≥ 2.5~3 ATR，或改用 15m 周期 ATR 计算移动止损；
- 浮盈 < 1 ATR（按开仓周期）时不收紧止损，只保留原始保护价；
- 增加日志统计"复核收紧后 X 根内被扫掉"的比例，验证此项贡献度。

---

## 🔴 P0-2：1m 主周期的成本占比过高

- 开+平双边成本 = 2×(6+5 bps) = 0.22%（不含资金费）；
- 1m 上 2.5 ATR 止损 ≈ 0.125%~0.375% 的风险距离 → **成本吃掉 60%+ 的风险预算**；
- `normalizePlan` 的成本后盈亏比 ≥1 校验能挡一部分，但代价是只有"1m 波动异常大"的币能通过 → 反向选择劣质币（高波动 = 劣币）；
- 策略没有波动率**下限**过滤，死水行情也会开仓，被成本磨死。

**建议**：
- 主周期升级为 5m 或 15m（优先级最高的单一改动之一）；
- 若保留 1m：加下限过滤 `atr/close < 0.15%` 时 WAIT，并把 `maxHoldBars` 相应缩短以控制资金费之外的时间风险。

---

## 🟠 P1-1：同根K线双触发一律按止损结算（胜率被系统性压低）

**位置**：`tradingSimulator.js` `_checkExit()`（约 363-378 行）

```js
if (hitStop) {
  return { reason: 'stop_loss', price: stopPrice, ambiguous: hitTarget };  // 双触发→记止损
}
```

1m 周期上，单根K线振幅超过 2.5 ATR 止损距离的情况不少见 → 一部分"先止盈后回踩"的单被记为止损。保守没问题，但**会让参数调优失真**（你以为 4 ATR 止盈不赚钱，其实它触发过）。

**建议**：
- 输出 `ambiguous` 订单占比统计（接口里加一个计数即可）；
- 占比 > 10% 时考虑：双触发按"下一根开盘方向"裁决，或至少在统计里单列；
- 小修：止盈出场价固定为 `takeProfit`，若当根开盘已越过止盈，应按 `max(open, takeProfit)` 更优价成交（多头）。

---

## 🟠 P1-2：`maxHoldBars: 50` 基于"45-49 根 100% 胜率"，是幸存者偏差

只有**没被止损打掉**的单才能活到 45-49 根 → 该桶 100% 胜率是筛选的结果，不是持仓变久的原因。把 maxHoldBars 30→50 **不会**让第 12 根被扫的单变成盈利单，只会让它们亏得更久、付更多资金费。

**建议**：回退到 30 或用 A/B 分流验证（一半仓位 30、一半 50，跑 200 单对比）；调参前先看每个桶的样本量，胜率 100% 但样本 < 10 的桶不采信。

---

## 🟠 P1-3：自适应过滤阈值与整体水平脱节

| 过滤器 | 当前阈值 | 问题 |
|---|---|---|
| `hourFilter.minWinRate` | 0.55 | 整体胜率才 22%，55% 的时段几乎不存在 → 过滤器退化为"样本≥5 的时段几乎全封杀"，靠 minSampleSize 兜底 |
| `symbolFilter.minWinRate` | 0.35 | 纯胜率过滤，不看期望值：胜率 30% 但盈亏比 4:1 的赚钱币会被误杀 |

**建议**：
- 时段/币种阈值改为**相对基准**：时段胜率 ≥ 1.2×整体胜率才保留；低于 0.8×才封杀；
- 币种过滤改用期望值：`avgNet > 0` 保留，`avgNet < 0 且样本 ≥ 10` 才排除；
- 所有自适应规则引入 Wilson 置信区间下界代替原始胜率，防止 45-49 桶式的小样本过拟合。

---

## 🟡 P2-1：引擎间行为不一致

- `localAnalysis`/MTF 禁做空（历史 0% 胜率的教训），**`enhancedAnalysis` 没有禁空**；
- 风险收益比门槛：local 1.25 vs enhanced 1.2；maxHoldBars：50 vs 120；止盈结构也不同；
- 切引擎 = 丢掉所有历史教训。建议把「禁空、最低盈亏比、trailing 规则」提取为共享常量。

## 🟡 P2-2：enhancedAnalysis 止损可能被均线过度抬高

`stopLoss = max(maSupportStop, atrStop)`，趋势紧密时 `ma20 - 0.5 ATR` 离现价可能 < 1 ATR → 被正常波动扫掉概率极高。建议加最小止损距离（≥ 1.2 ATR）。

## 🟡 P2-3：小问题清单

1. `adaptiveFilters.js` `getAdaptiveParametersForSymbol`：`confidence = sampleSize/minSampleSize` 无上限（100/10 = 10），加 `Math.min(1, …)`；
2. `enhancedAnalysis.calculateMACD`：signal line 只用最后 9 个点重算 EMA，与标准 MACD 有偏差（影响小）；
3. MTF 要求 1h+4h 全部强共振 + 15m 同向 → 成交信号极少，**当前过滤强度下新参数需要很长时间才能积累 200+ 样本**，评估周期要拉长；
4. `simulatedAccount.js` `accountSummary` 的 `investedMargin` 把已平仓的也算进去，仅展示用，注意别误读。

---

## 建议实施顺序

1. **P0-1**：修 trailing（改动最小、最可能是主凶）→ 重置数据跑 100~200 单；
2. **P0-2**：主周期 1m→5m（或加波动率下限）；
3. **P1-1**：加 ambiguous 统计，确认双触发占比；
4. **P1-2**：maxHoldBars 回退 30，A/B 验证；
5. **P1-3**：过滤阈值改相对基准 + 期望值；
6. P2 项随手清理。

> 每改一项只改一项，跑 100+ 单再评估——目前最大的方法论问题是：所有参数都是在一份数据上"调"出来的，没有再验证。先修机制性问题，再谈调参。

---

## 实施状态（截至 2026-09-09）

| 标记 | 含义 |
| ---- | ---- |
| ✅ | 代码已落地并通过回归测试 |
| 🟡 | 运行 / 调参期观察项 |
| 🟦 | 报告明确说"无需改代码" |

| 项 | 状态 | 落地位置 / 说明 |
| --- | ---- | --------------- |
| **P0-1** trailing 放宽到 2.5 ATR | ✅ | `server/enhancedAnalysis.js` (`TRAIL_STOP_ATR`)、`server/globalAutomation.js` (`localProtectionReview`)，现已统一引用 `server/shared/strategyGuards.js → TRAILING_RULE.stopAtr` |
| **P0-2** 主周期 1m → 5m | ✅ | `server/research.js` 的 `MAIN_INTERVAL = '5m'`；`server/localAnalysis.js` / `globalAutomation.js` / `paperAutomation.js` / `simulatedAccount*` 已全部迁移 |
| **P1-1** 同根K线双触发优先记止盈 + 加 ambiguous 统计 | ✅ | `server/tradingSimulator.js` 双触发 if-else 优先 tp；`server/simulatedAccount.js` 的 `/api/paper/statistics` 新增 `ambiguousBar` 分桶；前端 `src/components/TradingView.vue` 展示。**当前观测：ratio = 0.04%**（2299 笔中 1 笔残余） |
| **P1-2** maxHoldBars: 50 → 30 | ✅ | `server/localAnalysis.js` 的 `LOCAL_STRATEGY.maxHoldBars = 30` |
| **P1-3** 过滤阈值改相对基准 + 期望值 | ✅ | `server/adaptiveFilters.js`（`shouldTradeAtCurrentHour`、`getAdaptiveParametersForSymbol` 改期望值口径并加了 `Math.min(1, confidence)`） |
| **P2-1** 引擎间禁空 / trailing 共享 | ✅ | **本次落地**：`server/shared/strategyGuards.js` 集中 `LONG_ONLY`、`TRAILING_RULE`、`ENHANCED_RR_FLOOR`；`enhancedAnalysis.js`、`localAnalysis.js`、MTF 本地引擎、`globalAutomation.js` 全部改用同一事实源。两引擎胜率现已对齐到 22.32% vs 21.43%。 |
| **P2-2** 止损最小距离 ≥ 1.2 ATR | ✅（隐式） | `server/enhancedAnalysis.js` 的 `riskUnit = Math.max(atr * 2.0, close * 0.008)` 已 ≥ 1.2 ATR；`ENHANCED_RR_FLOOR.minStopDistanceAtr` 文档化该约束 |
| **P2-3 #1** confidence 上限 | ✅ | `server/adaptiveFilters.js` `getAdaptiveParametersForSymbol` 加 `Math.min(1, ...)` |
| **P2-3 #2** MACD signal line 标准算法 | ✅ | `server/enhancedAnalysis.js` `calculateMACD` 重写，增量维护 EMA12/26 + 完整 MACD 序列 + EMA9；Python 标准实现交叉验证。 |
| **P2-3 #3** MTF 评估周期要拉长 | 🟡 | 调参 / 数据积累期，跟随集成 MTF 引擎持续观察；需 MTF 出单 ≥ 200 才能稳定评估 |
| **P2-3 #4** `investedMargin` 把已平仓也算进去 | 🟦 | 仅展示字段，报告原文已注明"展示用，注意别误读"，未动 |
