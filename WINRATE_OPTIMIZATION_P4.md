# 胜率优化报告 · P4（2026-09-10）

> 数据口径：`simulated_orders` 全部已平仓模拟订单，共 **2467 笔**（入场时间 2026-09-08 ~ 09-10）。
> 全部为 `automatic=true`，由 GlobalAutomation 以 `enhanced` 引擎、固定保证金 100 USDT、杠杆 5x 下单。
> 所有分析脚本只读数据库，未做任何写入。

---

## 一、结论摘要

| 指标 | 优化前 | 优化后（同批入场点回放预期） |
|---|---|---|
| 胜率 | **21.9%** | **≈29%**（1m & ATR≥0.35% & RSI≥50 口径 29.5%） |
| 均单净盈亏 | **-1.18 USDT** | **≈-0.20 USDT** |
| 亏损速度 | 基准 | 下降约 **80%** |
| 交易频率 | 基准 | 保留 **≈54%**（ATR≥0.30% 通过率） |

**仍然没有转正。** 本轮把「明显错误的结构」修掉了，但**入场信号本身仍不具备正期望**——这是下一轮必须解决的问题（见第五节）。

---

## 二、诊断：三层证据

### 1. 结构性证据——亏损集中在"快速被止损"

平仓原因分布：

| reason | 笔数 | 占比 | 胜率 | 净盈亏 | 平均持仓 |
|---|---|---|---|---|---|
| **stop_loss** | **2105** | **85.3%** | **11.8%** | **-5536** | 11.6 根 |
| take_profit | 268 | 10.9% | 100% | +2684 | 18.5 根 |
| timeout | 94 | 3.8% | 26.6% | -63 | 38.3 根 |

持仓时长分桶（决定性）：

| 持仓根数 | 笔数 | 胜率 | 均单净盈亏 |
|---|---|---|---|
| <5 | 747 | **5.0%** | **-2.474** |
| 5–14 | 894 | **18.5%** | **-1.642** |
| 15–29 | 606 | 36.0% | -0.037 |
| 30–44 | 126 | 58.7% | **+2.229** |
| 45–59 | 50 | 60.0% | **+2.849** |
| ≥60 | 44 | 38.6% | 0.000 |

**66% 的订单活不过 14 根，而活过 15 根就开始赚钱。** 系统不是"选错方向"，而是"进场就被打掉"。

### 2. 成本不是主因——毛利本身为负

| 项目 | 金额（USDT） |
|---|---|
| 毛盈亏（不含手续费/滑点/资金费） | **-1479.47** |
| 手续费 | -1421.59 |
| 资金费 | -13.89 |
| **净盈亏** | **-2914.95** |

均单成本仅 0.576 USDT，毛利却已是 **-0.60 USDT/单**。所以"降频省成本"最多只能拿回一半，另一半必须靠**提高信号质量**。

### 3. 参数回放证明——止损/止盈怎么调都救不回来

在同一批真实入场点上回放 9 组止损/止盈/移动止损参数（V0~V8，含 1.0R/1.5R/2R/3.5R 止盈、1.5~3 ATR 止损、0.5R/1R 移动止损）：

| 变体 | 胜率 | 均单净盈亏 |
|---|---|---|
| V0 基线（2ATR 止损 / 3.5R 止盈 / 2.5ATR 移动） | 28.0% | -1.005 |
| V3 止盈降到 1.0R | 43.3% | -1.033 |
| V5 止损放宽到 3 ATR | 37.2% | -1.201 |
| V6 止损收到 1.5 ATR | 26.3% | **-0.914（最优）** |

**没有任何一组参数组合能转正。** 胜率可以被"放宽止损"买回来（V5→37.2%），但均单亏损反而更大；反过来收紧止损能减小亏损但胜率下滑。这说明**入场点的期望值本身接近 0 或为负**，出场参数只是在重新分配盈亏，无法创造 edge。

### 4. 出场前 MFE 诊断——入场时机确实偏差

按实际持仓根数截断统计"出场前最大有利偏移（MFE，以 R 为单位）"：

| 分类 | 平均出场前 MFE |
|---|---|
| 盈利单 | 1.93 R |
| 亏损单 | **0.34 R** |

亏损单的 MFE 分布：**38.8% 的亏损单连 0.1R 的浮盈都没出现过**（一入场就逆行），另有 17.5% 不足 0.25R。
→ 约 1/3 的入场点在方向上就是错的，属于"追势/逆势"问题，而不是止损过紧。

### 5. 最强单因子：波动率（ATR / 价格）呈**单调**关系

| ATR/价格 | 笔数 | 胜率 | 均单净盈亏 |
|---|---|---|---|
| <0.10% | 42 | 26.2% | -1.19 |
| 0.10–0.15% | 63 | 20.6% | -1.98 |
| 0.15–0.25% | 738 | **16.3%** | -1.32 |
| 0.25–0.40% | 692 | 19.9% | **-1.45** |
| ≥0.40% | 927 | 27.7% | -0.81 |
| ≥0.50% | 700 | 29.4% | -0.48 |
| ≥0.60% | 516 | **32.6%** | **+0.14** |

机理清楚：**低波动 = 震荡市，趋势信号没有跟随性，进场即被均值回归打掉止损。**
原代码的下限是 `0.0005`（0.05%），形同虚设（460 个币只拦掉 113 个）。

RSI 同样单调（RSI≥45 → -1.15 / ≥55 → -1.12 / ≥60 → -0.95 U/单），作为第二道闸门。

---

## 三、本轮落地的修改（3 处）

### 修改 1｜主周期回退 5m → 1m ⭐ 最关键

`server/research.js`：`MAIN_INTERVAL = '5m'` → `'1m'`

上一轮把 1m 升到 5m 的理由是"放大止损/止盈距离以降低成本占比"，**实盘证伪**：

| 周期 | 笔数 | 胜率 | 均单净盈亏 |
|---|---|---|---|
| 1m | 2227 | **22.8%** | **-0.913** |
| 5m | 235 | **13.6%** | **-3.727** |

按创建时间核对，5m 于 09-09 21:00 切换，此后**每个小时的净盈亏几乎全为负**（12 小时里 11 小时为负）。
原因：5m 的 ATR 约为 1m 的 2~3 倍 → 止损距离被同步放大 → 单笔绝对亏损放大，而胜率反而下降，
**把亏损速度加快了约 4 倍**。周期切换的收益应来自信号质量，不是距离放大。

### 修改 2｜新增波动率下限（0.05% → 0.30%）

`server/enhancedAnalysis.js`：新增 `NOFX_MIN_ATR_PCT`（默认 `0.003`）

通过率核对（1m 历史入场点）：≥0.20% 通过 82.7%，**≥0.30% 通过 53.6%**，≥0.35% 通过 43.8%，≥0.40% 通过 37.8%。
取 0.30%：保留过半交易流量，同时拿到大部分胜率收益。

该闸门**自带时段自适应**（这正是不设固定"交易时段"也能达到同样效果的原因）：

| UTC 时段 | 样本 | 平均 ATR% | ≥0.30% 通过率 |
|---|---|---|---|
| 00 点 | 200 | 0.523 | 69% |
| 04 点 | 115 | 0.457 | 53% |
| 12 点 | 83 | **0.162** | **7%**（死水时段，自动不做单） |
| 19 点 | 293 | 0.357 | 41% |
| 23 点 | 86 | 0.534 | 66% |

### 修改 3｜方向性 RSI 门槛收紧

`server/enhancedAnalysis.js`：
- 多单：RSI 下限 `40 → 50`（`NOFX_MIN_RSI_LONG`）
- 空单：RSI 上限 `60 → 55`（`NOFX_MAX_RSI_SHORT`）

含义：只在"动能方向与信号一致"时入场，砍掉"趋势刚成立但动能还弱"的抢跑单。

### 附带修复

- `server/enhancedAnalysis.js`：`validForBars 2→6`、`maxHoldBars 24→120`（周期回退后根数须同步还原，否则 6 分钟入场窗口会缩成 2 分钟）
- `tests/adaptiveFilters.test.js`：修正 2 个**陈旧用例**。用例仍按旧的"≥5 单即可拉黑"构造 6 单样本，而 P3 已把排除门槛改成 `MIN_SAMPLE_SIZE_TO_EXCLUDE = 10`，导致断言必失败。已把样本改为 12 单以命中当前语义。**测试套件现已全绿（119 通过 / 0 失败 / 7 跳过）。**

---

## 四、验证

1. **单元/回归测试**：`npm test` → 119 pass / 0 fail。
2. **参数 A/B（真实最新行情，460 个币）**：确认新闸门确实在生效、且出单标的波动率从 0.205% 抬到 0.596%。
3. **服务已重启**：PM2 `nofx-api` 干净启动（restarts=0），日志确认 `enhanced` 引擎 + `MAIN_INTERVAL=1m` 生效。

---

## 五、诚实的结论与下一步

**本轮没有把系统做成盈利的，只是把"明显错误的结构"修掉了。** 剩余问题：

1. **入场信号本身没有正期望**（毛利 -1479 USDT 已证明）。出场参数怎么调都救不回来（第二节第 3 点）。
2. 下一轮的方向应该是**换/改入场逻辑**，而不是继续调止损止盈。候选：
   - **回调入场**：当前是"信号后下一根开盘直接市价入"，等于每次都在短期高点接货。改为"等回踩到区间下沿/均线附近再入"（可复用 `_tryEntry` 的入场区间语义）。
   - **形态确认**：38.8% 的亏损单从未浮盈过，说明方向判断本身有偏差，需要引入结构确认（如高低点结构、量价背离）。
3. **观察期**：建议积累 **100+ 笔**新订单后用 `node scripts/analyze-sim-winrate.cjs` 复查，重点看：
   - 胜率是否站上 28%
   - 均单净盈亏是否改善到 -0.3 以内
   - 若**出单量过少**（安静行情被闸门拦太多），把 `NOFX_MIN_ATR_PCT` 调回 `0.0015`；
     若想更激进，调到 `0.004`。

---

## 附：本轮新增的分析脚本（均只读数据库）

| 脚本 | 用途 |
|---|---|
| `scripts/diagnose-excursion.cjs` | MAE/MFE 诊断，区分"入场时机差"与"止损管理差" |
| `scripts/variant-replay.cjs` | 同一批真实入场点上回放 9 组止损/止盈参数 + 出场前 MFE |
| `scripts/filter-search.cjs` | 入场特征（波动率/偏离/RSI/量比/小时）单因子与组合网格搜索 |
| `scripts/candidate-eval.cjs` | 候选配置评估 + 时间前后各半的稳健性检验 |
| `scripts/atr-by-hour.cjs` | 按 UTC 小时统计波动率通过率（判断时段效应） |
| `scripts/gate-ab.cjs` | 新旧参数在真实最新行情上的出单率 A/B |
