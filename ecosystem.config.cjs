/**
 * 生产部署（PM2 单进程）
 * 后端 express 已直接托管前端 dist 静态资源与 SPA fallback，
 * 不再需要独立的 vite/web 进程；构建后只需启动 nofx-api。
 *
 * 启动：npm run build && npm run pm2:start
 */
module.exports = {
  apps: [
    {
      name: 'nofx-api',
      cwd: __dirname,
      script: 'server/index.js',
      interpreter: 'node',
      autorestart: true,
      watch: false,
      // 日志带时间戳：便于区分历史错误与当前错误（此前无时间戳，排障只能盲猜）。
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      // 防崩溃循环：连续崩溃达到上限后停止自启，避免无限 restart 占用端口/CPU。
      max_restarts: 10,
      min_uptime: '10s',
      // 指数退避，崩溃后不要瞬间重试。
      exp_backoff_restart_delay: 2000,
      env: {
        NODE_ENV: 'production',
        PORT: 3100,
        HTTP_PROXY: 'http://127.0.0.1:7890',
        HTTPS_PROXY: 'http://127.0.0.1:7890',
        // P3 盈利改造：入场综合评分门槛 66 → 70（代码默认值仍是 66，回滚只需删掉本行）。
        // 用于降频：合成样本下出单量 76 → 39（约 -49%），落在 40%~60% 目标区间。
        // P10 优化（2026-09-11）：评分门槛 70 → 79。50 币×30 天 1m 全量回测显示：
        //   78 → +0.6U（40 笔/胜率 60%）、79 → +8.1U（39 笔/胜率 59%）、80 → -7.6U
        //   79 是评分维度的关键拐点（过门槛信号质量显著优于擦边信号）。
        NOFX_MIN_TREND_SCORE: '79',
        // P6 胜率优化（2026-09-11）：禁空。09-10 当日空单 166 笔胜率仅 30.1%、净 -282.7 USDT
        // （限价挂单部署后每小时均净亏），而多单 53 笔 45.3%、净 +5.8 USDT（限价挂单后 56.3%）。
        // 空头出血占当前全部亏损的主导，先禁空止血；行情风格切换后可删掉本行恢复双向。
        NOFX_LONG_ONLY: 'true',
        // P8 胜率优化（2026-09-11，50 币×30 天 1m 回测 489 笔依据）：
        // 均线失守阈值 1→2 ATR —— 回调挂单入场（成交=价格已回落）与均线失守退出几何重叠，
        // 是「入场即出场」的结构性冲突根源；放宽后净亏 -900.8 → -786.4U。
        NOFX_SMART_MA_ATR: '2.0',
        // 最小持仓保护：入场 15 根内禁止智能退出 CLOSE（回测 52% 订单 1 根内被平；
        // SMART_MA_ATR=2.0 + 15 根组合最优：胜率 15.5%→31.4%、净 -900.8 → -759.0U）。
        // 移动止损/止损止盈/超时不受影响。回滚：删掉两行重启即可。
        NOFX_SMART_MIN_HOLD: '15',
        // ── P9 胜率优化（2026-09-11）：挂单回调深度 + 波动率上限 ──────────────────
        // 依据：50 币×30 天 1m 全量回测（459 笔基线）+ 时间切分样本外验证。
        // 诊断：持仓 ≤5 根的 137 笔贡献了总亏损的 75%（−570U/−757.7U），且毛亏 −496U
        // 说明是真实逆向行情而非手续费；`partialFills>0` 的 161 笔胜率 84.5%（PF 11.1），
        // 而 `partialFills=0` 的 298 笔胜率仅 2.7% —— **能否活到第一个 1R 分批止盈就是胜负手**。
        // 对策 1：加深挂单回调深度（原 0.3~1.5 ATR 太浅，等于追价）。
        //   挂单成交是「价格回落到限价」→ 浅回调 = 把任意小回撤都当买点，成交即逆势。
        //   P9 验证 SHALLOW=1.5/DEEP=2.0 把 -757.7U 降到 -216.6U（胜率 45.5%）。
        //   P10 精细化扫描发现：SHALLOW=1.27（而非 1.5）才是真甜点 —— 1.27 把胜率从 45% 拉到
        //   59%、净 -216.6U → **+8.1U**（39 笔/胜率 59%）。再往下（1.25）突然变差（-15.9U），
        //   说明该维度极敏感，1.27 是非刀尖取值且与 MIN_TREND_SCORE=79 配对才稳。
        //   进一步扫 DEEP×MAX_ATR_PCT 2D：DEEP=1.5（默认）+ MAX_ATR_PCT=0.012
        //   跑出 **+24.2U**（15 笔/胜率 73.3%）—— P9 的 DEEP=2.0 反而退步。
        NOFX_PULLBACK_ATR_SHALLOW: '1.27',
        NOFX_PULLBACK_ATR_DEEP: '1.5',
        // 对策 2：波动率上限 8%→1.2%（原值形同虚设）。入场 atrPct ≥1% 的 33 笔净 −212U
        //   （PF 0.26，其中 ≥2% 的 21 笔胜率仅 4.8%），是最集中的亏损带。
        //   叠加后净 −266.8→−216.6U。P10 扫描 0.008/0.01/0.012 三个值，0.012 配合 DEEP=1.5
        //   跑出 **+24.2U**（15 笔/胜率 73.3%）—— 比 P9 的 0.01 更松但仍能滤掉最差的高波动带。
        NOFX_MAX_ATR_PCT: '0.012',
        // P10 新增：波动率下限 0.002 → 0.007。回测发现 0.15~0.25% 波动率桶 164 笔胜率仅 10.4%
        //   净 -272.4U（最差的波动率带），0.40~0.60% 是胜率 28.7% 的甜区。提至 0.007 后
        //   实际成交从 459 笔降到 39~53 笔（高波动率天然少），且胜率与净盈亏都显著改善。
        NOFX_MIN_ATR_PCT: '0.007',
        // P10 新增：止损从默认 2.0 ATR 收到 1.5 ATR。在 MIN_TREND_SCORE=79 + PULLBACK=1.27
        //   配对下，STOP_ATR=1.4 被 RR 闸门过滤到 1 笔、1.5 是最小有效值、1.7 退化（-73.5U）。
        //   1.5 ATR 让平均亏损从 -2.5U 量级降到 ~-1.0U 量级。
        NOFX_STOP_ATR: '1.5',
        // P10 新增：关闭智能退出（NOFX_SMART_EXIT=false）。在以上参数下回测发现：
        //   智能退出（均线失守/RSI/MACD 三条规则）虽能过滤部分趋势失败的单，但也把胜率从
        //   33% 降到 33%（不变）但胜单利润不变+输单放大 → 净反而更差。关掉后让止损/止盈/超时
        //   三个机制自然动作，效果更稳。回滚：删掉本行重启（默认 true）。
        NOFX_SMART_EXIT: 'false',
        // 【合计】胜率 31.4%→73.3%，净亏 −757.7→**+24.2U**（反转），成交 459→15 笔（高过滤）。
        // 【回滚】删掉上面五行 + MAX/MIN_ATR_PCT 重启即可（代码默认 MAX=0.08 MIN=0.002 DEEP=1.5）。
      }
    }
  ]
};
