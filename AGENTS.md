# 项目约定

## 回测工具链

- 所有新增、修复或重构的回测脚本必须使用 **Node.js**，并以 `.js`、`.mjs` 或 `.cjs` 文件形式保存在 `scripts/` 中。
- 回测必须复用项目既有的 JavaScript 引擎和领域逻辑（优先 `server/tradingSimulator.js`、策略分析模块与共享成本模型）；不得因执行该任务的模型或代理不同而改用 Python、Shell、临时脚本或其他语言重写同一套回测逻辑。
- 发现回测脚本错误时，应直接修复对应的 Node.js 脚本，并用 Node.js 语法检查及适用的测试或烟雾验证确认修复。
- Python 目录仅服务于独立桌面应用；它不是回测工具链的一部分。不要在其中新增回测实现。

