# 主测试套件的基准开关（tests/*.test.js）。
# 分批止盈（PARTIAL_TP，P7 默认开启）改变了成交/结算语义，
# 而 research.test.js / simulatedAccount.test.js 的大量用例固化的是
# 「全仓一次性止盈」的精确账目。这里关掉分批止盈，保持这些用例的
# 既有语义；分批路径由 scripts/_test_partial_tp.mjs（子进程）单独覆盖。
NOFX_PARTIAL_TP=false
