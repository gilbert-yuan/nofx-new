# 胜率优化 P6 报告（2026-09-11 01:00~01:15）

> 自动化例行任务：根据模拟下单结果优化胜率，有修改则重启服务并提交 git。

## 一、基线数据（干净 1m 样本）

P5 之后协作方连续落地了三个大版本：限价挂单+GTC（a02836c）、出场逻辑系统性重构（8173140）、SWR 缓存（3989df4）。本次复查按 P5 预定计划，取 **09-10 14:00 之后**的干净 1m 样本（已剔除 15m 对照实验单）：

| 口径 | 单数 | 胜率 | 净盈亏 | 均单 |
|---|---|---|---|---|
| 全部（14:00 起） | 134 | **37.3%** | -158.64 | -1.18 |
| 其中多单 | 42 | **54.8%** | **+49.89** | +1.19 |
| 其中空单 | 92 | 29.3% | **-208.53** | -2.27 |
| 10:16 起空单（更大样本） | 166 | 30.1% | -282.68 | -1.70 |
| 10:16 起多单 | 53 | 45.3% | +5.83 | +0.11 |

对照基线：P4 前 21.9% → P4 后 33.8% → 本轮干净样本 37.3%，**胜率持续改善但仍未转正**。

### 结构性发现

1. **空单是唯一大出血点**：10:16 起空单 166 笔净亏 -282.7U，占全部净亏损的绝大部分；20:00（限价挂单部署）之后空单**每个小时都净亏**（胜率 11%~31%），且 63 笔止损单均单 -3.25U。多单同期为正（限价挂单后 56.3% 胜率、+30.9U）。
2. **盈利依然靠持仓时间**：持仓 <5 根胜率 0%，5-14 根 29.8%，45-59 根 70%，≥60 根 64.5%——与 P4/P5 结论一致，出场重构（R 阶梯 + 3ATR 展盈）方向正确。
3. **止盈单质量极高**：5 笔 take_profit 均单 +18.8U；timeout 单 59.1% 胜率。问题依然是止损单太多（90 笔 SL vs 5 笔 TP，命中比 18:1）。
4. 00:00 时段 10 单仅 10% 胜率（样本太小，观察项，不动参数）。
5. 止损单平均不利幅度 0.474%——出场重构 23:14 才部署，其效果需重新累计样本。

## 二、本轮修改

### 1. 禁空：NOFX_LONG_ONLY=true（ecosystem.config.cjs）

空单 166 笔样本已过 100 笔门槛，且限价挂单前后两个时段空单均为负（旧入场 -0.39U/单、限价 GTC -3.0U/单），多空不对称是当前最确定的信号。启用共享护栏 `LONG_ONLY`（localAnalysis / enhancedAnalysis / strategyGuards 三处同源拦截），**砍掉当前最大出血源**。

- 预期：以 14:00 后样本静态折算，净盈亏 -158.64 → **+49.89**（仅保留多单）。
- 回滚：删除 `NOFX_LONG_ONLY` 一行并重启即恢复双向。

### 2. 修复 5 个陈旧测试（全部改为对齐 R 口径新行为）

- `tests/bidirectionalStrategy.test.js`：杠杆断言改用共享 `RISK_RULE`（预算 0.08 → 0.1 已漂移）+ 与生产一致的 ref（优先 entryLimit）；零 RSI 空单夹具补 `entryMin/entryMax`（R 口径需要可计算的风险单位）。
- `tests/enhancedAnalysis.test.js`：两个移动止损夹具补 `entryMin/entryMax`；"增强版复核-移动止损"行情从单边直涨改为阶梯上涨带回调（单边直涨 RSI 触顶 80 会触发智能退出获利了结，属新设计的正确行为，盖过了本用例要验证的路径）。
- `tests/globalAutomation.test.js`：止损距离断言从 `stopAtr×ATR` 固定距离改为 **R 阶梯档位**（`TRAILING_RULE.ladder`）口径。

`npm test`：141 例 **0 失败**（7 skipped 为有意设计）。

### 3. 落库协作方遗留改动（31 文件）

09-10 23:50 后工作区累积的未提交改动一并提交（限价挂单/GTC 收敛、链式分析、文档、回归脚本 `_test_chain_analysis.mjs` 等）——服务此前已运行该工作区代码，落库使 git 与生产一致。临时文件（`scripts/_tmp_*.cjs`、`out-winrate.txt`）与 `output/sanhe6/` 不入库。

## 三、服务重启

按规程执行：`npm run build` → `pm2 delete nofx-api` → 确认 3100 释放 → `pm2 start ecosystem.config.cjs`。
重启后 HTTP 200、restarts=0，进程 env 确认 `NOFX_LONG_ONLY=true` / `NOFX_MIN_TREND_SCORE=70` 生效。

## 四、下轮必读

1. **先 `git status`**：本轮已清空基线；若再现并发协作者改动，不要擅自 revert 其决策。
2. 禁空后样本口径变为"仅多单"，用 `scripts/analyze-post-p4.cjs`（SINCE 已改为 14:00）复查时按方向分组看。
3. **样本 <100 笔不改参数**。出场重构（8173140）后的新出场节奏需要独立样本，勿与旧数据混评。
4. 观察项（不动手）：00:00 时段胜率异常低；止损单平均不利幅度 0.474% 与 0.8% 初始止损下限的关系（多为 R 阶梯跟随止损触发，非初始止损被打）。
5. 若多单在禁空后仍持续 >100 笔净亏，下一主线回到入场质量（评分门槛 / MIN_ATR_PCT 复标），而不是再动出场。
