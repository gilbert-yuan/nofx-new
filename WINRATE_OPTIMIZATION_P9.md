# 胜率优化 P9：挂单回调深度 + 波动率上限

> 执行时间：2026-09-11 09:37–10:05（GMT+8）
> 数据基础：50 币 × 30 天 × 1m 全量回测（`data/backtest/`，52 币 K 线，43200 根/币）
> 结论：**胜率 31.4% → 45.5%，净亏 −757.7U → −216.6U（−71%）**，已通过时间切分样本外验证。

---

## 一、执行摘要

| 指标 | 基线（线上现行） | P9 优化后 | 变化 |
|---|---|---|---|
| 胜率 | 31.4% | **45.5%** | +14.1 pp |
| 净盈亏 | −757.7 U | **−216.6 U** | **−71%** |
| 均单净盈亏 | −1.651 U | **−0.624 U** | −62% |
| 成交笔数 | 459 | 347 | −24% |
| 挂单成交率 | 77.4% | 62.3% | −15 pp |
| 样本外段均单（后 40%） | −1.770 U | **−0.581 U** | −67% |

**改动仅 3 个环境变量 + 1 处可配置化，零策略逻辑重写。** 未转正（毛收益仍为负），但把结构性亏损压缩了七成。

---

## 二、诊断：钱亏在哪里

先看全量已平仓订单（2 742 笔，线上口径）：

```
止损单 2227 笔 (81%)  平均 −2.679 U/单  合计 −5967 U
止盈单  274 笔 (10%)  平均 +10.163 U/单 合计 +2785 U   ← 唯一赚钱的出场
超时单  212 笔         平均 −0.602 U/单
```

**止盈:止损 = 1 : 8.1，而盈亏比只有 3.79 : 1 → 结构性负期望。**
要打平需要胜率 ≈ 35.9%，实际 22.7%。

毛/费拆解：毛 −1755 U、手续费 1574 U、净 −3348 U。**手续费占 47%**，但毛本身已是负的——降费救不了。

### 回测口径下的三个决定性发现（459 笔，`r70.json`）

**1. 持仓 ≤5 根 = 全部亏损的 75%**

| 持仓 | 笔数 | 胜率 | 均单净 | 毛 |
|---|---|---|---|---|
| **0~5 根** | **137** | **10.9%** | **−4.164** | **−496.1** |
| 5~15 根 | 124 | 39.5% | −0.903 | −38.9 |
| 15~30 根 | 139 | 35.3% | −0.878 | −38.8 |
| 30~45 根 | 27 | 48.1% | **+0.657** | +34.3 |
| 45~60 根 | 17 | 58.8% | **+0.900** | +25.7 |
| ≥60 根 | 15 | 53.3% | **+0.911** | +23.0 |

137 笔（30%）吃掉 −570U，而全场合计只有 −757.7U。且这一组**毛亏 −496U、手续费仅 74U**
——是真实的价格逆向运动，不是摩擦成本。**能不能活过前 5 根，决定一切。**

**2. 分批止盈是唯一的正贡献来源**

| 分组 | 笔数 | 胜率 | 均单净 | 盈亏比 |
|---|---|---|---|---|
| `partialFills > 0` | 161 | **84.5%** | **+2.443** | **11.1** |
| `partialFills = 0` | 298 | 2.7% | −3.863 | 0.00 |

TP1 在 1R 平 40%，**只要摸到 1R 就基本锁定赢单**。所以问题不是"止盈设多远"，
而是"为什么 65% 的单连 1R 都摸不到就死了"。

**3. 评分、时段都救不了，杠杆结论是假的**

- 评分无区分度：score 80-85 / 85-90 / 90-100 胜率 29.2% / 29.2% / 31.6% → **抬高门槛无效**。
- 时段效应不存在：干净样本里本地 09-13 点胜率 40.0%，反而**高于**其余时段 27.8%，
  与旧结论"早间高密时段带偏"相反。另发现 `shouldTradeAtCurrentHour` 只在 `engine==='local'`
  分支执行，enhanced 引擎下是死代码 → **不做时段过滤**。
- ⚠️ 实盘数据里 lev1/lev2 看着 +0.750 / +0.529 U/单，但同种子回测为 −6.044 / −8.497 U/单
  → 小样本（n=27/61）假象。**杠杆不是"风险旋钮"，而是波动率代理
  （`lev = floor(0.1 / 止损距离%)`），别据此调参。**

---

## 三、根因：挂单回调深度太浅

入场是限价挂单：`entryLimit = close − depth · atr`。
`depth` 由评分线性映射：高分→浅回调（0.3 ATR，急于入场），低分→深回调（1.5 ATR）。

问题在于**浅回调 = 把任意小回撤都当买点**。挂单成交的定义就是"价格回落到限价"，
所以浅回调的成交几乎总是发生在**下行动能刚启动**时——成交即逆势，随即被 0.8% 的
`minStopPct` 地板止损打出，这正是"持仓 ≤5 根"那一组的来源。

横向验证：`waitBars`（挂出到成交的等待根数）越小越差
——0~2 根成交的 119 笔均单 −2.805 U（胜率 22.7%），等 15~31 根的只有 −0.977 U（胜率 40.8%）。
**越急着成交的单越亏。**

---

## 四、改进方案与扫描证据

### 4.1 加深回调深度（主改动，贡献 +491U）

`NOFX_PULLBACK_ATR_SHALLOW` 扫描（DEEP 固定 1.5）：

| SHALLOW | 笔数 | 胜率 | 净 U | 均单 |
|---|---|---|---|---|
| 0.3（基线） | 459 | 31.4% | −757.7 | −1.651 |
| 0.6 | 442 | 32.1% | −687.0 | −1.554 |
| 0.9 | 426 | 31.9% | −646.3 | −1.517 |
| 1.2 | 415 | 34.5% | −528.1 | −1.273 |
| **1.5** | 399 | 36.8% | −440.7 | −1.105 |

**单调改善** → 继续二维扫描：

| SHALLOW / DEEP | 笔数 | 胜率 | 净 U |
|---|---|---|---|
| 1.2 / 1.5 | 415 | 34.5% | −528.1 |
| 1.2 / **2.0** | 395 | 40.0% | −383.1 |
| **1.5** / 1.5 | 399 | 36.8% | −440.7 |
| **1.5 / 2.0** | 377 | **45.1%** | **−266.8** |
| 1.5 / 2.5 | 353 | 44.5% | −289.2 |
| 1.8 / 2.0 | 356 | 46.9% | −272.6 |
| 1.8 / 2.5 | 337 | 39.8% | −271.8 |

→ **最优区间 SHALLOW 1.5~1.8 / DEEP 2.0**，再加深进入平台期（成交笔数继续掉但收益不涨）。
选 **1.5 / 2.0**（深度区间 1.5~2.0 ATR）：保留略多成交（377 vs 356），收益等价。

### 4.2 波动率上限 8% → 1%（次改动，贡献 +50U）

入场 `atrPct ≥ 1.0%` 的 33 笔净 −212U（PF 0.26），其中 `≥2%` 的 21 笔胜率仅 **4.8%**
——最集中的亏损带，而代码里的硬编码上限 `MAX_ATR_PCT = 0.08` **形同虚设**。

在 SHALLOW=1.5 / DEEP=2.0 基础上扫描：

| MAX_ATR_PCT | 笔数 | 胜率 | 净 U |
|---|---|---|---|
| 0.08（旧值） | 377 | 45.1% | −266.8 |
| **0.010** | 347 | 45.5% | **−216.6** |
| 0.008 | 344 | 45.1% | −224.5 |
| 0.012 | 349 | 45.6% | −216.0 |

→ 0.008~0.012 是**平台（非刀尖）**，取中值 **1%**。

---

## 五、样本外验证（时间切分）

以前 60% 交易为训练段、后 40% 为测试段：

| 配置 | 训练段（275/213 笔） | 测试段（184/134 笔） |
|---|---|---|
| 基线 | 胜率 32.4%，均单 −1.571U | 胜率 29.9%，均单 −1.770U |
| P9 | 胜率 **44.1%**，均单 **−0.652U** | 胜率 **47.8%**，均单 **−0.581U** |

**改善在两段都成立，且测试段更强（−67% vs −59%）→ 不是对单一时段的拟合。**
ATR 上限取 0.008 / 0.010 / 0.012 三值结果几乎相同，进一步说明结论不在刀尖上。

---

## 六、已落地的生产变更

**代码（1 处）**
- `server/enhancedAnalysis.js`：`MAX_ATR_PCT` 由硬编码 `0.08` 改为
  `numFromEnv('NOFX_MAX_ATR_PCT', 0.08, 0.001, 0.5)` —— **默认值不变，行为零变化**，
  下调由环境变量驱动，便于灰度与回滚。
- `server/shared/entryModel.js`：补标定注释（代码默认值仍为 0.3/1.5，便于"删行即回滚"）。

**配置（`ecosystem.config.cjs`，3 行）**
```
NOFX_PULLBACK_ATR_SHALLOW: '1.5'
NOFX_PULLBACK_ATR_DEEP:     '2.0'
NOFX_MAX_ATR_PCT:           '0.01'
```

**验证**
- `npm test`：**136 通过 / 0 失败**。
- 服务按规程重启（`delete` → 确认 3100 释放 → `start ecosystem`），`/api/health` ok，restarts=0。
- 运行进程环境变量已确认注入全部 7 个 `NOFX_*`。
- 回调深度实测：score 70→2.00 ATR、100→1.50 ATR（区间 1.50~2.00）。

### 回滚
删掉 `ecosystem.config.cjs` 里那 3 行并重启即可 —— 代码默认值（0.3 / 1.5 / 0.08）
即旧行为，无需改代码。

---

## 七、风险与后续

1. **仍未转正**：净亏从 −757.7 降到 −216.6 U，但毛收益仍为负。**1m 周期是否具备
   alpha 这一根本问题未被本轮解决**（与 02:11 可行性审计 t=−12.17 一致）。
2. **成交笔数下降 24%**（459→347），线上实盘挂单量也会同步下降；当前市况波动率偏低
   （多数币 1m ATR < 0.20% 被 `MIN_ATR_PCT` 挡掉），实盘出单可能偏少。
   若一周后实盘样本不足 100 笔，优先考虑下调 `NOFX_MIN_ATR_PCT`（0.002→0.0015）
   而不是回调本次改动。
3. **候选后续项（本轮未做，需 ≥100 笔新样本再评估）**
   - `partialFills=0` 的 298 笔胜率仅 2.7% —— 值得研究"入场后 N 根内未摸到 0.3R 就主动离场"
     以压缩尾部亏损（属于新增出场逻辑，须回测验证）。
   - stop_loss 仍占 80%（367/459），止损口径（`NOFX_STOP_ATR`/`NOFX_MIN_STOP_PCT`）
     尚未在 P9 基础上重扫。
4. **验证纪律**：本轮的改动应在实盘累计 ≥100 笔新平仓订单后，用
   `node scripts/analyze-sim-winrate.cjs` 与 `scripts/_post_p8_check.cjs "<P9 部署时间>"` 复核。

---

## 附：本轮新增/复用的工具

| 脚本 | 用途 |
|---|---|
| `scripts/_bt_sweep.mjs` | 参数扫描器（复用协作方成果），50 币约 53s/组 |
| `scripts/_bt_analyze.mjs` | 交易级多维归因：atrPct / score / 杠杆 / 持仓 / 出场 / 分批止盈 / 时段 / waitBars |
| `scripts/_bt_split.mjs` | 时间切分样本外验证（前 60% 训练 / 后 40% 测试） |
| `scripts/_post_p8_check.cjs` | 任意时点后的实盘模拟单诊断 |
| `scripts/_hour_effect.cjs` | 时段效应稳定性检验（结论：不存在） |
| `scripts/_cost_diag.cjs` / `_lev_diag.cjs` / `_struct_diag.cjs` / `_funnel_diag.cjs` | 成本拆解、杠杆混杂、出场结构、下单漏斗 |
